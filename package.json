{
  "name": "lfs-ambient-sounds",
  "version": "0.0.0",
  "main": "dist/index.cjs",
  "type": "module",
  "scripts": {
    "dev": "bun run ./src/index.ts",
    "compile": "bun build ./src/index.ts --compile --minify --sourcemap --target=bun-windows-x64 --outfile dist/lfs-ambient-sounds.exe",
    "format": "prettier --write ."
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "node-insim": "^4.6.3",
    "node-web-audio-api": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "prettier": "^3.5.3",
    "tsup": "^8.4.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  },
  "files": [
    "**/*.js",
    "**/*.d.ts"
  ]
}
