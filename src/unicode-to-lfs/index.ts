// TODO
export default function unicodeToLfs(
  value: string,
  options: {
    isNullTerminated?: boolean;
    length?: number;
    shouldEscapeSpecialCharacters?: boolean;
  } = {},
): string {
  return value;
}
